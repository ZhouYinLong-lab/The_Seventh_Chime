# 《黑潮钟》网页产品、技术与制作规范 v1.0

> 上位文档：`黑潮钟_完整游戏策划_v1.md`  
> 客观时空基线：`黑潮钟_身体时段地点矩阵_v1.md`  
> 文档用途：统一玩家端信息架构、查询交互、内容数据、静态站点技术方案、自动校验、制作流水线与测试验收标准  
> 产品形态：固定故事、非线性档案调查、静态叙事推理网页游戏  
> 部署目标：GitHub Pages，无后端、无账号、离线本地存档

---

## 0. 产品边界与不可违背原则

### 0.1 产品承诺

玩家通过查询已经发生的固定事件，逐步还原：

- 七具肉体在七个钟后时段的位置；
- 七个灵魂的轮转规律；
- 第四声后的版框篡改；
- 第七声后的替换式谋杀与逃亡。

玩家改变的是自己的认知和档案解锁顺序，不改变历史。

### 0.2 不做什么

- 不做实时移动、潜行、资源管理或回合策略。
- 不允许玩家选择灵魂如何跳转。
- 不根据玩家选择生成不同事件或结局。
- 不让系统自动完成灵魂身份、改版者或终局责任链等核心推理。
- 不用穷举所有“时段 × 地点 × 肉体组合”作为主要推进手段。
- 不在 B4 前以 UI 文案、字段名、图标或无障碍标签泄露“灵魂交换”。
- 不把口癖作为单独成立的身份答案。

### 0.3 产品设计底线

```text
客观地点由档案确认；
灵魂身份由玩家推理；
规则演算只在玩家证明规则后执行；
错误反馈指出矛盾维度，不代替玩家给出答案。
```

### 0.4 两份上位文档的优先级

- 世界观、人物、交换规则、真相和证据原则以完整策划为准。
- 具体肉体地点、场景分组、边界移动和物品路径以身体时段地点矩阵为准。
- 完整策划 v1.1 已同步地点矩阵状态；若场景分组或秒级空间顺序仍有冲突，以身体时段地点矩阵 v1.1 为准。

---

# 一、玩家信息架构

## 1.1 一级结构

玩家端包含六个一级区域：

| 区域 | 功能 | 是否一直可见 |
|---|---|---|
| 调查台 | 选择时段、地点和在场肉体，发起档案查询 | 是 |
| 档案阅读器 | 阅读当前记录、检查附件、标记证据 | 是 |
| 档案库 | 浏览已解锁档案、筛选、固定和回访 | 是 |
| 调查笔记 | 自由文本、手动标签、档案引用 | 是 |
| 客观地点表 | 查看已被档案确认的“肉体—时段—地点” | 是 |
| 灵魂假设 | 建立身体内灵魂假设、提交圆环和改版结论 | B4 正式开启 |

设置、存档和帮助属于工具层，不占一级调查导航。

## 1.2 页面整体布局

桌面端采用“查询—阅读—推理”三栏工作台：

| 区域 | 推荐宽度 | 内容 |
|---|---:|---|
| 左栏：调查台 | 24% | 时段、地点、肉体选择，查询历史，当前可验证线索 |
| 中栏：档案阅读器 | 48% | 正文、对话、动作、附件、证据标注 |
| 右栏：推理工作台 | 28% | 地点表、笔记、证据板；B4 后加入灵魂假设 |

顶栏固定显示：

- 游戏标题；
- 当前档案编号；
- 已解锁档案数；
- 保存状态；
- 设置；
- 导出存档。

底部不设置持续滚动的任务列表，避免把推理降格为清单打勾。当前阶段目标仅在玩家主动打开“调查方向”时显示。

## 1.3 窄屏与移动端

移动端改为单栏分页，不把三栏强行压缩：

1. `查询`
2. `档案`
3. `地点`
4. `笔记`
5. `假设`（B4 后出现）

要求：

- 页面切换保留滚动位置和未提交查询条件。
- 档案中的证据片段可长按或通过按钮加入笔记。
- 表格使用横向滚动和固定首列，不缩小到不可读字号。
- 竖屏可完整通关，但桌面端是首要体验与首轮 QA 基准。

## 1.4 初始信息量

初次进入只展示：

- B0 及当前允许查询的早期时段；
- 五个地点名称和简化地图；
- 七具肉体的公开姓名、肖像和身份；
- 2–4 份序幕档案；
- 无“灵魂”“占据”“锚点”“圆环”等术语。

地点表可以存在空格，但灵魂表不能以锁定空表的形式提前出现；否则玩家会从结构猜出机制。

---

# 二、核心查询循环

## 2.1 Type Help 式查询单位

每次查询由三部分构成：

```text
钟后时段 + 主地点 + 一组在场肉体
```

示例：

```text
B2 + 档案区 A + Mara、Kovač、Verri 肉体
```

灵魂不参与查询，也不进入玩家可见文件名。

## 2.2 查询操作

1. 玩家选择 B0–B7 中已开放或可输入的时段。
2. 选择五个主地点之一。
3. 从七具肉体中选择一至三具；顺序不影响匹配。
4. 点击“检索记录”或使用键盘提交。
5. 系统规范化查询键，并检查是否对应一份可发现档案。
6. 成功时打开档案；首次发现时更新档案库和客观地点表。
7. 玩家阅读、标记证据、建立新假设，再发起下一次查询。

## 2.3 查询匹配规则

查询键使用规范化集合：

```ts
queryKey = `${bell}:${location}:${sortedBodyIds.join('+')}`
```

硬规则：

- 肉体集合必须与该主要场景的物理在场者完全一致。
- 远程发言者不计入物理在场肉体集合。
- 空地点机械日志可用空集合查询，但必须由其他档案明确提示其存在。
- 尾声 P 不进入常规查询器；它作为终局档案附件解锁。
- 同一个规范化查询键最多对应一份主要档案，附件归属于该档案。

## 2.4 查询可发现性

并非数据库中所有真实记录一开始都可通过暴力枚举得到。档案有三种状态：

| 状态 | 行为 |
|---|---|
| `hidden` | 即使输入正确组合也不解锁；尚未获得必要线索 |
| `queryable` | 正确组合可解锁 |
| `discovered` | 已解锁，可从档案库直接打开 |

一份记录变为 `queryable` 的条件只能来自：

- 前置档案；
- 已完成的阶段性推理提交；
- 明确的系统教学节点。

不能以玩家现实时间、刷新次数或随机数改变可发现性。

## 2.5 防穷举设计

- 每条推进链至少先暴露三个查询维度中的两个。
- 肉体候选集合通常不超过三人。
- 成功档案必须给出至少一个新的可验证方向。
- 连续错误查询不扣资源、不锁死进度。
- 对同一组条件仅改变肉体顺序不算新的尝试。
- 查询历史合并同义重复项，避免形成噪声。
- 不显示“本时段尚有 N 个未发现档案”，以免玩家靠计数扫表。

## 2.6 查询结果的四种响应

| 响应 | 适用情况 | 玩家反馈 |
|---|---|---|
| 成功发现 | 键匹配且已可查询 | 打开档案，记录发现来源 |
| 已发现 | 已存在于档案库 | 直接打开，不重复播放发现动画 |
| 查询有效但无记录 | 组合合法但没有对应主要记录 | “没有找到符合这些条件的主要记录。” |
| 与已知事实冲突 | 已确认某具肉体不在该地点 | 指出冲突格，不透露正确地点或灵魂 |

系统不能用不同措辞暗示隐藏档案是否真实存在。

---

# 三、B4 前后界面状态变化

## 3.1 B4 前：身体调查模式

界面只使用以下词汇：

- 人物档案；
- 在场者；
- 身体特征；
- 行为异常；
- 未解释情报；
- 记录矛盾。

玩家可做：

- 查询档案；
- 查看地点表；
- 给句子和动作加证据标签；
- 写自由笔记；
- 把两份档案并排比较；
- 将异常暂时关联到某个“人物档案”。

玩家不可做：

- 在正式 UI 中指定身体内的灵魂；
- 查看圆环演算；
- 看到“灵魂技能”之类会直接泄露机制的系统分类。

B4 前内部证据标签的玩家文案应使用中性版本：

| 作者标签 | B4 前玩家文案 | B4 后玩家文案 |
|---|---|---|
| `soul_skill` | 异常技能 | 灵魂技能 |
| `private_memory` | 不可能情报 | 关系／记忆 |
| `continuing_intent` | 跨记录行为链 | 连续意图 |
| `body_constraint` | 身体限制 | 身体限制 |

## 3.2 B4 揭示触发

“灵魂假设”不以游戏进度百分比或档案数量机械解锁，而在玩家首次阅读 B4 原始圆环校样档案后开启。

触发流程：

1. 档案正文完成揭示。
2. 系统短暂强调此前姓名标签旁的“肉体识别”来源。
3. 已有档案的说话者标签统一补充小字“记录按肉体识别”。
4. 右栏新增“灵魂假设”。
5. 原有中性证据标签更新为明确术语，但不自动给旧证据绑定具体灵魂。
6. 给出一次不可跳过的简短操作教学：把一个候选灵魂拖入一个身体格；随后撤回示例，不保留答案。

揭示动画应短于 20 秒，并允许减少动态效果。

## 3.3 B4 后：双层调查模式

界面同时呈现两层：

```text
客观层：何时、何地、哪具肉体、什么物品；
假设层：肉体中的灵魂、意图、知识与错误信念。
```

视觉规则：

- 身体始终使用人物肖像和实线边框。
- 灵魂使用姓名缩写与独立图形纹理，不能只靠颜色区分。
- 未确认假设使用虚线；玩家已提交且通过的阶段结论使用实线。
- “原始圆环”和“实时版框”必须是两个不同对象，不可用同一图覆盖。

## 3.4 系统演算的边界

允许自动演算：

- 玩家正确提交原始圆环后，按已确认规则生成 B1–B4 的占据结果。
- 玩家正确提交“B4、Verri、Niko 肉体锚定、新六人环”后，生成 B5–B7 的理论结果。

不允许自动演算：

- 根据几条证据自动猜出原始圆环；
- 根据提前回归自动指出改版者；
- 自动选择被锚定肉体；
- 自动填写终局死者灵魂、开枪者认知或逃亡者；
- 把正确演算结果直接等同于所有人物动机结论。

自动演算是玩家证明规则后的算术减负，不是推理替代。

---

# 四、档案、笔记、地点表与灵魂假设交互

## 4.1 档案阅读器

每份档案页面包含：

1. 档案编号与标题；
2. 时段；
3. 地点；
4. 物理在场肉体；
5. 来源与可信度说明；
6. 正文／对话／动作；
7. 附件；
8. 玩家标注；
9. 由本档案引出的查询历史。

来源类型包括：

- 对话转录；
- 动作观察；
- 私人文本；
- 机械日志；
- 环境物证；
- 远程通讯。

说话者姓名始终标注肉体身份。B4 后可以悬停或聚焦查看玩家当前灵魂假设，但默认正文不替换说话者姓名。

## 4.2 档案库

档案库支持：

- 按时段筛选；
- 按地点筛选；
- 按在场肉体筛选；
- 按来源和玩家证据标签筛选；
- 只看未读／已固定／有自定义笔记；
- 两份档案并排比较；
- 回到首次发现该档案的查询。

档案库不支持按真实灵魂筛选，除非玩家已经在自己的假设中指定；该筛选结果必须明确标为“依据你的假设”。

## 4.3 调查笔记

笔记由三部分组成：

- 自由文本；
- 引用档案或具体段落；
- 玩家自定义标签。

要求：

- 自动保存，但保留撤销与重做。
- 支持将同一档案段落引用到多条笔记。
- 删除笔记不删除档案标注。
- 导出存档时完整包含笔记。
- 系统不对自由文本做答案识别，也不向外发送文本。

## 4.4 证据标注

玩家可以选择句子、动作或附件区域并标注：

- 身体限制；
- 异常技能／灵魂技能；
- 不可能情报／关系记忆；
- 连续意图；
- 机械事实；
- 可能撒谎；
- 规则原件；
- 规则可能过时。

标签是整理工具，不是自动判题输入。系统可以高亮同类证据，但不能据此自动建议某个灵魂。

## 4.5 客观地点表

地点表采用 `肉体 × B0–B7`：

- 已由解锁档案或机械日志确认的格子自动填入。
- 未确认格显示空白，不显示候选答案数量。
- 点击格子可查看支持该地点事实的档案。
- 玩家可写临时铅笔假设，但与系统确认值视觉区分。
- 临时假设与已确认事实冲突时只提示冲突，不给出正确值。

地点表不要求玩家重复填写已经由数据库直接确认的客观事实。其作用是空间索引和查询跳板，而非主要判题板。

## 4.6 灵魂假设表

灵魂假设表采用 `肉体 × B1–B7`，每格可放：

- 一个主要候选；
- 最多两个次要候选；
- 不确定标记；
- 支持与反对证据引用。

交互规则：

- 平时编辑不即时判定对错。
- 同一时段把一个灵魂放入两具肉体时，提示“你的假设违反一一对应”，但不指示哪格错误。
- 玩家可以锁定自己确信的格，防止拖拽覆盖。
- 系统只在阶段性“提交推论”时判定必要结论。
- 正确提交原始圆环后，系统显示由该规则演算的 B1–B4；这部分标记为“按你已证明的规则推导”。
- 后续演算与玩家临时候选分层显示，不相互覆盖。

## 4.7 原始圆环与实时版框

B4 后提供两个独立工具：

### 原始圆环

- 玩家排列七个名字形成闭环。
- 提交时只检查相对顺序，允许整体旋转等价。
- 方向错误时反馈“轮转方向与已知记录冲突”，不显示正确方向。

### 实时版框

- 玩家需要指出改版发生时段、改版者、被移除名字和其余六人的顺序。
- “校样”和“实时版框”在视觉上使用不同材质和标题。
- 原始圆环的正确答案不会自动复制到实时版框。

## 4.8 终局答卷

终局采用语义字段，而非自由长文判定：

| 字段 | 需要提交的内容 |
|---|---|
| 尸体肉体 | Verri 肉体 |
| 死亡灵魂 | Niko |
| 开枪者灵魂 | Kovač |
| 开枪时肉体 | Kovač 肉体 |
| 开枪者以为目标是谁 | Verri 灵魂 |
| 逃亡灵魂 | Verri |
| 逃亡肉体 | Niko 肉体 |
| 篡改者 | Verri |
| 锚定肉体 | Niko 肉体 |

最后再要求玩家从已解锁证据中各选一项：

- 身体／地点硬证据；
- 灵魂正向指认证据；
- 因果连续证据。

这一步确认玩家不是只靠猜对字段。

## 4.9 同场多证据源对齐

B7 的 R 电讯区是一场物理事件，但由多种来源共同记录：

- 秒级机器日志；
- 带噪音频／转录；
- Kovač 枪套封条状态；
- Livia 压在封条下的 Niko 私人验证卡；
- 受害者名单的发现位置；
- 发送带启动和发射完成记录。

档案阅读器需提供“时间对齐”视图：

- 横轴为同一场景内的秒级相对时间；
- 每一行是一种证据来源；
- 玩家可以展开原始内容并手动放置事件标记；
- 系统只对机器日志中的客观时间点自动定位；
- 证词、音频中的说话者意图和灵魂身份均由玩家解释；
- 不自动生成“谁先做了什么”的因果总结；
- 不因对齐视图引入新的时段或第二场景。

该视图用于重建“发送带启动—名单接近发报台—私人验证被噪声破坏—枪套破封—枪响—发送完成”的秒级顺序，但不能直接显示谁的灵魂位于哪具肉体。

## 4.10 B2 档案区三人场景的认知负载

B2 A 区虽然有 Mara、Kovač、Verri 三具肉体同场，但首次阅读只要求玩家形成两组强身份判断；第三具肉体只给弱线索，不在同一节点要求确认。

UI 处理：

- 正文按两段主要交互和一段边缘动作分组；
- 边缘动作可标注，但不显示为“待解决的第三项任务”；
- 阶段提交只检查两项强身份；
- 第三人的线索在后续跨档案比较时才成为正向证据；
- 提示系统不得诱导玩家在 B2 立即解完三人身份。

---

# 五、提示、错误反馈与卡关恢复

## 5.1 提示目标

提示只帮助玩家回到可推理状态，不替玩家说出核心答案。

提示分四级：

| 级别 | 内容 | 示例 |
|---|---|---|
| 1. 方向 | 指向未充分利用的档案或维度 | “比较同一具肉体在 B1 与 B2 的行为。” |
| 2. 范围 | 缩小到时段、地点或两三具肉体 | “B2 的医疗区能排除一种普通解释。” |
| 3. 矛盾 | 指明两项事实不能同时按现有假设成立 | “该灵魂在 B1 不可能知道 A 区发生的对话。” |
| 4. 操作 | 给出下一次查询需要的两个已知维度 | “尝试以 B3 和审讯区为固定条件。” |

不提供“正确灵魂是 X”或“把 X 放进 Y 肉体”的直接提示。

## 5.2 提示解锁条件

- 玩家主动请求，不自动弹出答案提示。
- 同一推进节点连续 8 次无效查询后，可低调显示“需要调查方向？”入口。
- 15 分钟未发现新档案或未完成阶段提交时，可显示一级提示入口。
- 已请求一级提示后，至少阅读或操作一次，才允许请求下一级。
- 无倒计时惩罚，不因使用提示影响结局。

## 5.3 错误查询反馈

反馈优先级：

1. 格式问题；
2. 与已确认客观地点冲突；
3. 当前线索尚不足；
4. 有效但无记录。

“当前线索尚不足”不能透露该组合将来是否有档案，只提示玩家继续调查当前可用记录。

## 5.4 推理提交反馈

阶段提交后最多返回两类维度反馈：

- 一一对应冲突；
- 轮转方向冲突；
- 身体限制冲突；
- 地点唯一性冲突；
- 信息获得时间冲突；
- 连续意图断裂；
- 使用了已过时规则。

反馈不定位具体错误格，除非该格与系统已确认的客观事实直接冲突。

## 5.5 防止软锁

- 所有解锁依赖必须为有向无环图或带明确入口的阶段图。
- 任何必需档案不得只由玩家手工笔记或标签触发。
- 删除笔记、改动假设、关闭页面不会丢失档案解锁状态。
- 导入较旧版本存档时执行迁移，不得让已发现档案重新锁定。
- 提示系统始终至少能指向一个当前可执行动作。

---

# 六、内容数据规范

## 6.1 数据分层

源数据分为三层：

| 层 | 用途 | 是否部署到玩家站点 |
|---|---|---|
| `author/` | 完整客观真相、灵魂、知识、物品、答案 | 否 |
| `content/` | 玩家可见档案、文案、附件和解锁条件 | 是，经构建编译 |
| `runtime/` | 构建生成的索引、查询键、版本与资源清单 | 是 |

静态网页无法从决心检查源代码的玩家手中绝对隐藏答案，但正式构建不得主动携带作者母表、终局答案或未使用注释。

## 6.2 标识符规则

- 使用 ASCII 小写 `snake_case`。
- 标识符发布后不重用。
- 人物：`mara`、`klara`、`livia`、`niko`、`mateo`、`kovac`、`verri`。
- 地点：`h_admin`、`r_radio`、`j_medical`、`a_archive`、`c_bell`、`p_epilogue`。
- 时段：`b0` 至 `b7`。
- 场景：`scene_b2_a_01`。
- 档案：`doc_b2_a_mara_kovac_verri`。
- 物品：`item_kovac_pistol`。
- 结论：`claim_b4_niko_body_verri_soul`。

显示名与标识符分离，避免改文案破坏存档兼容。

## 6.3 玩家可见档案 schema

```json
{
  "id": "doc_b2_a_mara_kovac_verri",
  "schemaVersion": 1,
  "title": "死亡名册旁的争执",
  "bell": "b2",
  "location": "a_archive",
  "physicalBodies": ["mara", "kovac", "verri"],
  "remoteBodies": [],
  "sourceType": "observed_transcript",
  "sourceReliability": "medium",
  "query": {
    "requiredBodies": ["mara", "kovac", "verri"],
    "orderIndependent": true
  },
  "discoverability": {
    "initialState": "hidden",
    "becomesQueryableWhen": {
      "all": [
        {"docDiscovered": "doc_b1_a_mara_kovac"},
        {"factKnown": "fact_verri_body_moved_to_a_b2"}
      ]
    }
  },
  "content": [
    {
      "id": "seg_001",
      "type": "dialogue",
      "speakerBody": "verri",
      "text": "……"
    },
    {
      "id": "seg_002",
      "type": "action",
      "text": "Verri 的肉体把名单折入外套夹层。"
    }
  ],
  "attachments": ["asset_victim_list_fragment"],
  "playerTagOptions": [
    "unexplained_knowledge",
    "continuing_intent",
    "mechanical_fact"
  ],
  "onFirstDiscover": {
    "confirmFacts": [
      "fact_mara_body_at_a_b2",
      "fact_kovac_body_at_a_b2",
      "fact_verri_body_at_a_b2"
    ],
    "makeQueryable": ["doc_b3_h_mateo_kovac_verri"]
  }
}
```

## 6.4 场景 schema

场景是客观物理单位，档案是玩家看到的媒介。通常一场景对应一份主要档案，也可带多个附件。

```json
{
  "id": "scene_b2_a_01",
  "bell": "b2",
  "location": "a_archive",
  "physicalBodies": ["mara", "kovac", "verri"],
  "primaryDocument": "doc_b2_a_mara_kovac_verri",
  "optionalAttachments": ["asset_victim_list_fragment"],
  "isPrimaryPhysicalScene": true
}
```

## 6.5 人物与地点公开 schema

```json
{
  "characters": [
    {
      "id": "niko",
      "displayName": "Niko Petrović",
      "publicRole": "钟楼学徒",
      "portrait": "portraits/niko.webp",
      "publicBodyTraits": ["17 岁", "身形瘦小"],
      "pronunciationNote": "尼科·彼得罗维奇"
    }
  ],
  "locations": [
    {
      "id": "c_bell",
      "shortCode": "C",
      "displayName": "礼拜堂／钟楼",
      "adjacentTo": ["h_admin"],
      "specialEdges": [
        {
          "to": "a_archive",
          "requiresBody": "niko"
        }
      ]
    }
  ]
}
```

## 6.6 作者真相 schema

作者真相按“时段中的肉体状态”记录，不把灵魂、地点、意图拆散到无法审计的多份文档。

```json
{
  "id": "truth_b4_niko_body",
  "bell": "b4",
  "body": "niko",
  "soul": "verri",
  "location": "c_bell",
  "intent": "alter_live_frame_and_anchor_niko_body",
  "knowledgeBefore": [
    "knows_original_ring",
    "knows_live_frame_editing",
    "knows_niko_body_fits_maintenance_shaft"
  ],
  "falseBeliefsBefore": [],
  "physicalConstraints": [
    "niko_small_frame",
    "niko_young_stamina"
  ],
  "actions": [
    "remove_niko_slug",
    "reorder_remaining_six",
    "conceal_niko_slug_on_body"
  ],
  "interactsWithBodies": [],
  "remoteInteractions": [],
  "itemsIn": ["item_niko_name_slug"],
  "itemsOutCarried": ["item_niko_name_slug"],
  "itemsOutLeftAtLocation": ["trace_frame_scratch"],
  "knowledgeAfter": ["knows_new_six_ring"],
  "beliefsAfter": ["believes_escape_plan_is_viable"],
  "supportsClaims": [
    "claim_verri_altered_frame",
    "claim_niko_body_anchored"
  ]
}
```

## 6.7 物品状态 schema

```json
{
  "id": "item_kovac_pistol",
  "initialState": {
    "carrierBody": "kovac",
    "location": "h_admin",
    "seal": "intact"
  },
  "transitions": [
    {
      "boundary": "b6_to_b7",
      "carrierBody": "kovac",
      "from": "h_admin",
      "to": "r_radio",
      "seal": "intact"
    },
    {
      "at": "b7",
      "action": "seal_broken_and_fired",
      "byBody": "kovac",
      "bySoul": "kovac"
    }
  ]
}
```

## 6.8 知识与信念 schema

```json
{
  "actorSoul": "kovac",
  "at": "b6",
  "knows": [
    {
      "fact": "kovac_expected_to_return_at_b7",
      "source": "truncated_warning_b6"
    }
  ],
  "believes": [
    {
      "claim": "all_souls_return_at_b7",
      "basis": ["mara_simplified_rule", "partial_return_observed"],
      "isObjectivelyTrue": false
    }
  ],
  "doesNotKnow": [
    "niko_slug_removed",
    "verri_soul_anchored_in_niko_body"
  ]
}
```

## 6.9 证据结论 schema

```json
{
  "id": "claim_b4_niko_body_verri_soul",
  "statement": "B4 的 Niko 肉体中是 Verri 灵魂",
  "requiredForCompletion": true,
  "evidenceTriangle": {
    "positive": ["evidence_verri_private_code"],
    "exclusion": ["evidence_niko_could_not_know_frame_editing"],
    "causalContinuity": ["evidence_verri_searches_frame_b3_b4"]
  },
  "minimumDistinctCategories": 3
}
```

---

# 七、静态 GitHub Pages 技术架构

## 7.1 推荐技术栈

- Vite；
- TypeScript 严格模式；
- Preact 或轻量 React 兼容层；
- CSS Modules 或原生层叠样式；
- Zod 用于运行时数据校验；
- Vitest 用于单元与内容规则测试；
- Playwright 用于端到端测试；
- Markdown 内容在构建时转换为安全 HTML。

选择 Preact 的理由是组件模型清晰、静态包体小、无需服务器。若团队更熟悉原生 TypeScript，可以替换 UI 层，但数据结构与校验要求不变。

## 7.2 目录结构

```text
black-tide-bell/
├─ src/
│  ├─ app/
│  ├─ components/
│  ├─ features/
│  │  ├─ query/
│  │  ├─ archive/
│  │  ├─ notes/
│  │  ├─ location-grid/
│  │  ├─ soul-hypothesis/
│  │  └─ save/
│  ├─ state/
│  ├─ content-runtime/
│  ├─ a11y/
│  └─ styles/
├─ content/
│  ├─ documents/
│  ├─ attachments/
│  ├─ characters.json
│  ├─ locations.json
│  └─ unlocks.json
├─ author/
│  ├─ occupancy.json
│  ├─ locations.json
│  ├─ knowledge.json
│  ├─ items.json
│  ├─ claims.json
│  └─ answers.json
├─ scripts/
│  ├─ build-content.ts
│  ├─ validate-content.ts
│  └─ verify-no-author-data.ts
├─ public/
├─ tests/
│  ├─ unit/
│  ├─ content/
│  └─ e2e/
└─ .github/workflows/deploy.yml
```

## 7.3 构建流程

```text
作者真相 + 玩家内容
        ↓
内容一致性校验
        ↓
解锁图与查询键编译
        ↓
剥离作者字段和答案
        ↓
Vite 静态构建
        ↓
产物泄露扫描
        ↓
GitHub Pages 部署
```

## 7.4 GitHub Pages 注意事项

- `base` 必须与仓库路径一致，例如 `/black-tide-bell/`。
- 路由优先使用哈希路由或单页回退兼容方案，避免直接刷新 404。
- 所有资源使用相对 `base` 的 URL，不写站点根绝对路径。
- 部署工作流只上传构建产物，不上传 `author/`。
- 构建失败或校验失败时禁止部署。
- 内容版本写入 `build-info.json`，用于存档兼容检查。

## 7.5 状态管理

采用单向状态更新。运行时状态分为：

- 内容索引：只读；
- 玩家进度：可持久化；
- UI 临时状态：不必持久化；
- 推导显示缓存：可重建，不写入永久存档。

不把“正确答案”存入前端全局状态。阶段判题可由经过最小化的验证规则完成；如果答案必须存在于静态客户端，应拆分和压缩，但不把混淆当成安全保证。

## 7.6 性能预算

初始加载目标：

- 首屏压缩后脚本小于 250 KB；
- 初始内容索引小于 150 KB；
- 肖像与附件按需加载；
- 首次可交互时间在普通移动网络下目标小于 3 秒；
- 已打开档案切换小于 100 ms；
- 存档写入小于 50 ms。

这些是试玩优化目标，不得以牺牲文本清晰度或无障碍为代价。

---

# 八、存档、localStorage 与导入导出

## 8.1 存档内容

```ts
interface SaveGameV1 {
  saveVersion: 1;
  contentVersion: string;
  createdAt: string;
  updatedAt: string;
  discoveredDocIds: string[];
  readDocIds: string[];
  queryableDocIds: string[];
  knownFactIds: string[];
  queryHistory: QueryRecord[];
  locationPencilMarks: Record<string, string | null>;
  soulHypotheses: SoulHypothesisState;
  submittedDeductions: SubmittedDeduction[];
  notes: PlayerNote[];
  annotations: EvidenceAnnotation[];
  pinnedDocIds: string[];
  hintState: HintState;
  accessibility: AccessibilityPreferences;
  playtestLog?: LocalPlaytestEvent[];
}
```

不保存：

- 作者真相；
- 系统正确答案；
- 可从已提交结论重新演算的缓存；
- 完整未解锁档案正文。

## 8.2 localStorage 键

```text
btb.save.v1.current
btb.save.v1.backup
btb.settings.v1
btb.playtest.v1.events
```

写入流程：

1. 验证内存状态；
2. 序列化；
3. 先写 `backup`；
4. 再原子替换 `current`；
5. 更新界面保存状态。

浏览器 `localStorage` 不提供真正事务，故恢复时优先选择能通过 schema 校验且 `updatedAt` 更新的版本。

## 8.3 自动保存时机

- 首次解锁档案；
- 添加、修改或删除笔记；
- 改动地点铅笔标记或灵魂假设；
- 提交阶段推理；
- 请求提示；
- 修改无障碍设置；
- 页面隐藏前。

连续编辑笔记时使用 500–1000 ms 防抖，不在每次按键同步写入。

## 8.4 导出格式

导出为 UTF-8 JSON 文件：

```text
black-tide-bell-save-YYYYMMDD-HHmm.json
```

导出文件包含：

- 文件格式说明；
- 存档主体；
- 完整性校验值；
- 内容版本；
- 不包含玩家设备、浏览器指纹或任何账号信息。

## 8.5 导入流程

1. 本地读取文件，不上传网络。
2. 校验 JSON、`saveVersion` 和必需字段。
3. 校验 ID 是否属于当前内容版本。
4. 显示将覆盖的存档摘要。
5. 用户确认后保留旧存档至 `backup`。
6. 执行迁移并重新计算所有派生状态。
7. 任一步失败则恢复旧存档。

## 8.6 版本迁移

- 内容新增允许旧存档直接加载。
- 档案 ID 改名必须提供映射表。
- 删除 ID 时保留墓碑记录，避免导入失败。
- 改变解锁规则时不得撤销玩家已经发现的档案。
- 主版本不兼容时提供“以只读方式查看旧进度”或重新开始选项。

---

# 九、构建期一致性校验器

## 9.1 校验器定位

`validate-content.ts` 是发布门禁，不是可选辅助脚本。任何错误级问题都必须阻止构建。

命令建议：

```text
npm run validate:content
npm run test
npm run build
npm run verify:dist
```

## 9.2 时空与占据校验

必须检查：

1. B0–B7 每具肉体恰有一个地点。
2. B1–B7 每具肉体恰有一个灵魂。
3. B1–B7 每个灵魂恰占据一具肉体。
4. 每个时段七肉体、七灵魂均守恒。
5. 相邻时段的肉体移动至多一条合法边。
6. C—A 特殊边只由 Niko 肉体使用。
7. P 不进入常规查询矩阵。
8. 每具肉体每时段最多一场主要物理事件。
9. 场景在场肉体与地点矩阵一致。
10. 远程参与者不能误记为物理在场者。

## 9.3 交换规则校验

- B1–B4 结果必须由原始七人圆环演算得到。
- B5–B7 必须由 Niko 肉体锚定和六人新环演算得到。
- B4 后 Verri 灵魂持续在 Niko 肉体。
- B7 Verri 肉体必须为 Niko 灵魂。
- B7 Kovač 肉体必须为 Kovač 灵魂。
- B7 的终局答卷必须与母表一致。

## 9.4 物品连续性校验

对每件关键物品逐边检查：

- 物品只有一个位置或一个携带肉体。
- 时段内不会跨地点。
- 边界移动只能随其携带肉体移动。
- 转交必须发生在双方同地点的时段。
- 物品不会在同一时刻既留在地点又被携带。
- 配枪封条、名单夹层、Niko 活字和发送带的状态变化顺序正确。

## 9.5 知识与错误信念校验

对每项行为所需知识检查：

- 灵魂在行动前是否已经获得信息；
- 信息来源是否同场、远程或由灵魂从上一身体携带；
- 是否错误地让灵魂读取肉体原主记忆；
- 私密信息是否能被合理泄露；
- 角色的误信是否有可观察来源；
- 角色是否在新证据出现后仍无理由地维持旧信念。

## 9.6 档案与查询校验

- 每个规范化查询键唯一。
- 场景时段、地点和肉体集合与查询键一致。
- 所有 `unlocks`、`factKnown` 和附件引用存在。
- 没有隐藏档案因错误条件永久不可达。
- 必需档案的解锁图无循环依赖。
- 从新存档出发至少有一个可执行查询。
- 任一非终局状态至少存在一个可推进动作或可用提示。
- B4 灵魂 UI 触发档案唯一且可达。
- B4 前内容不得出现受禁术语或带剧透的无障碍标签。

## 9.7 证据公平性校验

每个必需结论：

- 至少有一个正向指认证据；
- 至少有一个排除证据；
- 至少有一个因果连续证据；
- 三类证据至少来自两份不同档案；
- 至少一项不依赖口癖；
- 所有必需证据在提交节点前可达；
- 结论不存在只有作者内心动机才能成立的跳步。

## 9.8 发布产物泄露检查

扫描 `dist/`：

- 不得包含 `author/` 路径。
- 不得包含 `answers.json`、`occupancy.json` 或作者注释。
- 不得包含 `bySoul`、`correctSoul` 等作者字段名。
- 不得包含终局答案整句的测试快照。
- source map 在正式发布中关闭或单独审查。
- 构建日志与错误报告不能复制未公开正文。

---

# 十、可访问性与中文文本呈现

## 10.1 键盘与焦点

- 全流程可仅用键盘完成。
- 查询三步具有明确 Tab 顺序。
- 肉体多选支持空格切换，不强迫拖拽。
- 圆环和灵魂假设提供“选择源—选择目标”的非拖拽替代操作。
- 对话框关闭后焦点回到触发按钮。
- 新档案打开时将焦点移动到档案标题，并通过礼貌级实时区域播报。
- 不设置键盘陷阱。

## 10.2 屏幕阅读器语义

- 身体姓名必须朗读为“肉体识别：Mara”，B4 前只朗读玩家已知表述。
- B4 后灵魂假设朗读为“玩家假设，不是已确认事实”。
- 表格包含行列标题与时段说明。
- 图标和纹理均有文本替代。
- 钟声次数不能只通过音频表达；同步显示“第四声钟”等文本。
- 摩尔斯节奏提供等价文字或可检查的视觉节拍，不要求玩家仅靠听力识别。

## 10.3 视觉与动态

- 正文对比度至少达到 WCAG AA。
- 身体、灵魂、确认、假设不能只靠颜色区分。
- 支持 200% 浏览器缩放。
- 最小正文 16 px，建议中文档案正文 17–18 px。
- 提供高对比度模式。
- 遵循 `prefers-reduced-motion`；揭示动画可立即完成。
- 闪电和故障效果不得高频闪烁。

## 10.4 中文排版

- 首选字体栈：系统中文无衬线用于 UI，衬线字体可用于档案正文，但必须保证可读性。
- 正文行宽控制在约 32–42 个汉字。
- 行高 1.65–1.8。
- 段落之间使用间距，不依赖首行空格。
- 中文与拉丁姓名、数字之间保持一致的自动间距策略。
- 人名首次出现显示原文和中文音译，之后保持同一写法。
- 不在行首出现句末标点，不让成对引号拆得难以辨认。
- 长电报码、编号和文件 ID 允许换行或横向滚动。

## 10.5 外国姓名与术语

人物卡首次展示：

```text
Mara Vuković
玛拉·武科维奇
法庭译员
```

玩家可在设置中选择正文说话者显示：

- 中文音译；
- 拉丁原名；
- 中文音译 + 拉丁原名。

无论显示方式如何，存档和查询均使用稳定 ID。

## 10.6 音频非必需原则

- 气氛音、钟声和电报音可以增强体验，但不能承载唯一证据。
- 所有音频证据有精确文本转录。
- 提供独立的音乐、环境音和界面音音量。
- 静音状态下可完整通关。

---

# 十一、内容制作流水线

## 11.1 单场景制作顺序

每个场景严格按以下顺序制作：

1. 锁定母表格：时段、地点、肉体、灵魂。
2. 填写每个灵魂进入场景前的知识、误信、目标和底线。
3. 确认携带物、地点物和身体限制。
4. 只写客观行为，不写修辞化台词。
5. 标记场景要支持的局部结论、长期谜团和人物揭示。
6. 配置证据三角中的角色。
7. 写玩家可见档案正文与附件。
8. 指定查询线索和解锁依赖。
9. 运行自动校验。
10. 进行一次不看作者真相表的编辑审读。

## 11.2 场景卡模板

```text
场景 ID：
时段／地点：
物理在场肉体：
远程参与肉体：
真实灵魂：
每人进入前已知：
每人进入前误信：
每人当下目标：
身体限制：
进入物品：
客观动作顺序：
离开物品：
玩家可见来源：
本场局部问题：
推进的长期谜团：
人物意志揭示：
正向证据：
排除证据：
因果连续证据：
前置查询线索：
后继查询线索：
可选气氛内容：
```

## 11.3 台词写作规范

- 先确保知识边界，再塑造语言风格。
- 灵魂决定词汇、句法、称呼、价值判断和停顿习惯。
- 肉体决定声线、呼吸、发音器官限制和疲劳。
- 每个灵魂至少有三种语言特征，但不固定为单一口癖。
- 伪装行为必须记录“角色为什么会伪装、知道模仿到什么程度、在哪里露馅”。
- 对话不替玩家总结机制。
- 任何关键事实都不只埋在一段长台词中。

## 11.4 档案编辑规范

- 核心档案建议 300–900 个中文字。
- 单份档案最多承担一个主要新机制概念。
- 长档案使用小标题、时间戳或附件分隔。
- 每份核心档案至少承担两项职责。
- 纯气氛内容放入可选附件，不阻塞主链。
- 每个新专有名词首次出现有上下文解释。
- 每轮编辑后更新证据结论表，而不是只改正文。

## 11.5 变更控制

任何以下变更必须重新运行全量校验和终局演算：

- 身体地点；
- 灵魂占据；
- 边界移动；
- 物品携带者；
- 角色获得信息的时段；
- 圆环顺序；
- 解锁依赖；
- B6 截断警告措辞；
- B7 枪击动作顺序。

只修改修辞、错字或标点时也要运行内容引用检查，但不必重做人工因果评审。

---

# 十二、试玩指标与研究方法

## 12.1 隐私原则

正式版默认无网络遥测。试玩版可在本地记录匿名事件，并由试玩者主动导出。自由笔记正文默认不进入试玩日志。

## 12.2 核心指标

| 指标 | 目标区间 | 说明 |
|---|---:|---|
| 首次成功查询时间 | 3–8 分钟 | 验证查询教学是否可理解 |
| 首次提出“身份／意识错位”假说 | 20–35% 进度 | 太早则伏笔过硬，太晚则矛盾不足 |
| B4 揭示时已有该假说的玩家 | 50–80% | 揭示应验证推理而非凭空告知 |
| B4 揭示后能正确解释姓名字段 | ≥85% | 验证核心概念理解 |
| 原始圆环首次提交正确率 | 30–60% | 过高可能太机械，过低可能证据不足 |
| 识别“校样不等于实时版框”比例 | ≥70% | 第二层谜题的关键理解 |
| 终局正确识别死亡灵魂 | ≥65% 无四级提示 | 核心公平性指标 |
| 无效查询占比 | 15–35% | 太低过度引导，太高组合空间失控 |
| 单次卡关持续时间 | 中位数 <15 分钟 | 超出需检查依赖或提示 |
| 核心档案回读次数 | 中位数 ≥2 | 验证重新解释旧档案的体验 |
| 提示使用率 | 一级 30–60%，四级 <15% | 高级提示过多代表结构问题 |
| 完成时长 | 3–5 小时 | 与内容规模目标一致 |

## 12.3 试玩事件日志

可记录：

- 查询条件与结果类别；
- 档案首次打开、回访和停留时长；
- 阶段提交次数及反馈维度；
- 提示请求级别；
- B4 UI 解锁时间；
- 导出、导入和恢复事件；
- 崩溃或内容校验错误。

不得记录：

- 自由笔记正文；
- 玩家姓名、IP、设备指纹；
- 剪贴板内容；
- 与游戏无关的浏览记录。

## 12.4 试玩访谈问题

- 你在什么时候第一次怀疑“名字不是行动者本人”？
- 哪两份证据让你最确信？
- B4 的揭示是在确认、惊喜，还是强行改规则？
- 你何时意识到原始校样可能过时？
- 你为什么认为 Kovač 会开枪？
- 你是否能区分肉体能力、灵魂技能和地点权限？
- 哪次查询像合理推理，哪次像猜组合？
- 哪个界面替你做了太多，哪个界面又不够用？

---

# 十三、QA 测试矩阵

## 13.1 功能与内容 QA

| 类别 | 核心用例 | 通过标准 |
|---|---|---|
| 查询规范化 | 以不同肉体选择顺序提交同一组合 | 命中同一档案，不重复计数 |
| 物理／远程区分 | B3 传声会议 | 远程肉体不进入地点表和查询集合 |
| 唯一地点 | 任取一具肉体与一个时段 | 只有一个系统确认地点 |
| 档案解锁 | 正确查询隐藏但未就绪档案 | 不泄露其存在，获得前置后可解锁 |
| B4 UI | 阅读正式揭示档案 | 灵魂假设出现，旧档案标签语义更新 |
| 原始圆环 | 提交旋转等价排列 | 判为同一闭环；反向排列不通过 |
| 改版推理 | 正确指定 B4、Verri、Niko 锚定 | 只演算 B5–B7，不自动回答终局 |
| 终局答卷 | 正确字段但证据不足 | 要求选择证据三角，不直接完成 |
| 提示 | 多次无效查询后请求提示 | 只给当前级别，不泄露答案 |
| 笔记 | 新建、引用、删除、撤销 | 内容稳定，档案状态不受影响 |

## 13.2 存档 QA

| 场景 | 通过标准 |
|---|---|
| 首次档案解锁时关闭页面 | 重开后档案仍已解锁 |
| 编辑长笔记后立即切标签 | 防抖写入完成，无文字丢失 |
| `current` 损坏但 `backup` 正常 | 自动恢复并提示 |
| 导入旧内容版本 | 执行映射，已发现档案不回锁 |
| 导入非法 JSON | 不覆盖现有存档 |
| 导入缺少必需字段 | 明确报错并保持原状态 |
| 存储空间不足 | 告知失败，允许立即导出 |
| 多标签页同时编辑 | 检测版本冲突，要求选择较新存档或导出两份 |

## 13.3 可访问性 QA

| 场景 | 通过标准 |
|---|---|
| 键盘完成一次查询 | 无需鼠标，焦点顺序合理 |
| 键盘完成圆环排列 | 有非拖拽方案 |
| 屏幕阅读器阅读地点表 | 正确播报行、列、确认／假设状态 |
| 静音游玩摩尔斯相关档案 | 所有证据有文字等价物 |
| 色觉模拟 | 身体、灵魂、确认、假设仍可区分 |
| 200% 缩放 | 无关键控件遮挡或横向页面溢出 |
| 减少动态效果 | B4 揭示无强制动画或闪烁 |

## 13.4 浏览器与设备 QA

最低覆盖：

- 最新两个主版本 Chrome；
- 最新两个主版本 Firefox；
- 最新两个主版本 Edge；
- Safari 当前主版本和前一主版本；
- iOS Safari；
- Android Chrome；
- 1366×768 桌面；
- 1920×1080 桌面；
- 390×844 竖屏；
- 768×1024 平板。

## 13.5 剧情与推理 QA

人工逐项检查：

- 每个角色行为能从当时知识、误信、目标和底线推出。
- 没有人因使用肉体而读取原主记忆。
- 所有关键物品连续存在。
- B2 继电器破坏能客观造成 B6 截断，不依赖巧合。
- Niko 灵魂在 B7 取名单的目的有前置证据。
- Kovač 的误杀需要局部真话、本人回归和动作误读三项共同成立。
- Livia 未阻止枪击由地点和水密门解释，而不是角色突然失能。
- P 只证明逃离方向，不产生新的常规时段。
- 所有红鲱鱼在终局都有动机解释。

## 13.6 发布冒烟测试

- GitHub Pages 根路径与子路径资源均正常。
- 刷新当前档案页面后能恢复工作台。
- 新存档可完成第一份查询。
- B4 揭示、原始圆环提交、改版提交和终局答卷均能到达。
- `dist/` 不含作者真相数据。
- 导出文件可在另一浏览器导入。
- 无网络时已加载会话可继续阅读与整理；若启用离线缓存，版本更新提示正确。

---

# 十四、开发里程碑

## M0：规则冻结与数据骨架

交付：

- 确认上位两份策划为内容基线；
- 建立人物、地点、时段、场景、档案、真相、知识、物品和结论 schema；
- 手工录入完整占据矩阵与地点矩阵；
- 校验器完成守恒、地点唯一性和邻接检查。

退出条件：母表可机器读取，全部时空校验通过。

## M1：无美术交互原型

交付：

- 调查台；
- 档案阅读器；
- 档案库；
- 基础地点表；
- localStorage；
- 6–8 份占位档案。

退出条件：玩家无需口头说明即可完成三次查询和一次档案回访。

## M2：最小垂直切片

交付内容见第十五章。

退出条件：小规模试玩验证“异常—假说—B4 揭示—双层重读”完整体验。

## M3：作者工具与全量内容骨架

交付：

- 35 个物理场景全部建卡；
- 关键物品与知识／误信逐时段表；
- 解锁依赖图；
- 证据三角覆盖报告；
- 产物泄露检查。

退出条件：无完整台词也能用客观行为摘要跑通固定真相。

## M4：Alpha

交付：

- 全部核心档案初稿；
- B4 前后完整 UI；
- 原始圆环、实时版框、终局答卷；
- 分级提示；
- 导入导出；
- 本地试玩日志。

退出条件：开发团队外玩家可以从新存档通关，无人工解锁干预。

## M5：Beta

交付：

- 文本二稿与附件美术；
- 无障碍完整实现；
- 浏览器适配；
- 内容难度与提示校准；
- 存档迁移演练。

退出条件：无 P0/P1 缺陷；核心试玩指标进入目标区间。

## M6：发布候选

交付：

- 最终文本和校对；
- 构建产物泄露审计；
- GitHub Pages 部署工作流；
- 隐私说明、玩法帮助、版本信息；
- 完整回归测试。

退出条件：连续两次干净构建和全量测试通过，候选版存档可跨构建加载。

---

# 十五、最小垂直切片

## 15.1 切片目的

切片不是短篇外传，也不引入分支。它从正式固定故事中抽取最少内容，验证四件事：

1. 玩家理解“时段 + 地点 + 肉体集合”的查询方式。
2. 前期矛盾能让玩家主动提出“行动者与身体可能分离”。
3. B4 揭示能重新解释旧档案，而非像作者临时加规则。
4. B4 后灵魂假设工具提供整理能力，但不会替玩家作答。

## 15.2 内容范围

建议使用正式故事中的：

- 七张公开人物卡；
- 五地点简图；
- B0 基线档案 3 份；
- B1 软矛盾档案 3 份；
- B2 成对错位档案 3 份；
- B3 圆环前兆档案 1 份；
- B4 原始校样揭示档案 1 份；
- B4 后可重读验证档案 1–2 份；
- 总计 12–13 份档案；
- 一个简化阶段答卷：确认两项关键灵魂错位并排列原始圆环的局部片段。

不包含：

- B4 改版完整答案；
- B7 谋杀真相；
- 终局逃亡；
- 正式结局文本。

切片中的所有档案必须来自正史，不能为试玩另造与正式规则不同的三人版契约。

## 15.3 必须实现的功能

- 查询台完整交互；
- 档案库与双档案比较；
- 证据标注与自由笔记；
- 客观地点表；
- B4 前中性术语；
- B4 揭示后灵魂假设开启；
- 基础阶段提交和维度反馈；
- localStorage；
- 存档导出／导入；
- 键盘操作和静音可通关；
- 本地试玩事件导出。

## 15.4 切片通过标准

至少 8 名未参与设计的试玩者中：

- 6 人无需口头指导完成首次查询；
- 5 人在 B4 前提出行动者／身体分离假说；
- 7 人在 B4 后能解释“档案姓名只识别肉体”；
- 5 人能用至少两种证据说明一个灵魂错位；
- 无人因存档、查询或界面状态丢失而无法继续；
- 四级提示使用人数不超过 2 人；
- 至少 5 人主动回读两份以上旧档案。

若未达标，优先调整线索和信息架构，不先增加更多档案。

---

# 十六、验收清单

## 16.1 产品验收

- [ ] 故事完全固定，无实时或分支逻辑。
- [ ] 查询键仅使用时段、地点和肉体。
- [ ] B4 前不泄露灵魂术语。
- [ ] B4 后工具帮助整理，不自动识别灵魂。
- [ ] 地点事实与灵魂假设清楚分层。
- [ ] 玩家可重读并重新解释旧档案。
- [ ] 终局要求解释误杀和逃亡，而不只填“凶手姓名”。

## 16.2 技术验收

- [ ] GitHub Pages 可直接部署。
- [ ] `author/` 不进入发布产物。
- [ ] 内容校验错误会阻止构建。
- [ ] localStorage 损坏可从备份恢复。
- [ ] 存档可以导出、导入和迁移。
- [ ] 键盘、屏幕阅读器、静音和 200% 缩放可完成主流程。
- [ ] 主流桌面与移动浏览器通过冒烟测试。

## 16.3 内容制作验收

- [ ] 35 个物理场景均有场景卡。
- [ ] 每个场景的知识、误信、目标、身体限制和物品已填写。
- [ ] 每个关键结论具备证据三角。
- [ ] 所有物品有连续状态路径。
- [ ] 所有必需档案在需要提交前可达。
- [ ] 所有红鲱鱼有角色动机解释。
- [ ] B6 截断警告与 B7 枪击顺序通过人工因果审查。

---

# 十七、当前制作决策摘要

已确定并纳入本规范：

- 固定故事、类《Type Help》的查询推理循环；
- 五个主地点、B0–B7、时段内单一地点；
- 七具肉体与七个灵魂一一对应；
- 身体地点矩阵 v1.0 作为当前空间基线；
- Klara 肉体固定 R、Livia 肉体固定 J；
- 两具固定肉体保留为“地点和身体恒定、灵魂变化”的控制实验轴；
- B2 A 三人场景只要求两组强身份，第三人保留弱线索；
- B4 正式揭示灵魂机制并开启假设工具；
- 原始校样与实时版框分离；
- B7 R 以秒级多源记录重建，界面支持时间对齐但不代替灵魂判断；
- 客观地点自动记录、核心灵魂推理由玩家完成；
- 纯静态 GitHub Pages 架构；
- 本地存档与手动导入导出；
- 作者真相不进入部署产物；
- 自动校验作为内容发布门禁；
- 先完成垂直切片，再扩写 35 个场景的正式文本。

本规范的下一份配套文档应是：

> 《黑潮钟》35 场景档案目录与解锁依赖图 v1.0

它只负责给每个物理场景编号、定义档案媒介、前置查询线索、后继查询线索和证据职责，不应再次修改身体地点矩阵。
