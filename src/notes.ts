import { documents } from './content';
import type { SaveV2 } from './types';
const esc = (value: string) => value.replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[char] ?? char);
export const notesPanel = (state: SaveV2) => `<section class="panel compact"><h2>调查笔记</h2><label for="note-text">写下可检验的观察</label><textarea id="note-text" rows="5" placeholder="例如：同一肉体在 B0 与 B1 的操作方式是否一致？"></textarea><button data-action="add-note">保存笔记</button><ul class="notes">${state.notes.map((note, index) => `<li><p>${esc(note.text)}</p><small>${note.refs.map((ref) => `${esc(documents.get(ref.docId)?.title || ref.docId)}${ref.segmentId ? ` · ${esc(ref.segmentId)}` : ''}`).join('；')}</small><button class="quiet" data-action="delete-note" data-index="${index}">删除</button></li>`).join('') || '<li class="muted">还没有笔记。</li>'}</ul></section>`;
